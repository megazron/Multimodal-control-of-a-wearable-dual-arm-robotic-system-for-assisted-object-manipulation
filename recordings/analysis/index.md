# Results figures

Generated by `scripts/make_results.py` on 2026-08-30 07:02. Every figure names its source recording and date; a figure whose source is missing is listed under REFUSED with the reason, never drawn empty.

## Figures

* **accuracy** (`accuracy_per_mode.png`): Per-mode grasp positioning and T1 placement error, from recordings/verification/accuracy_table.json (2026-08-24; clip set re-recorded 2026-08-23). PREDATES the 2026-08-26 VR smoothing.
* **grasp_matrix** (`grasp_matrix.png`): Grasp success per mode x task from accuracy_table.json (2026-08-24). T1/T1S2 run under 06 only; T0/D* grasp nothing by design.
* **vr_smoothing** (`vr_smoothing.png`): 1-Euro vs the EMA it replaced vs raw: stillness residual and reach lag, recomputed live through src/srl_vr_teleop/srl_vr_teleop/vr_smoothing.py (filter maths on constructed signals, the method of test_vr_smoothing.py; smoothing landed 2026-08-26). NOT a through-the-node or real-arm measurement -- that is measure_vr_smoothing.py, which needs ROS up.
* **vr_protocol** (`vr_protocol_20260826.png`): Per-segment hand vs arm distance, direction error and peak lag from recordings/vr_teleop/protocol_20260826_124739.json (2026-08-26, sim-only). The file holds per-segment aggregates, not time series, so no path or error-vs-time plot is possible from it; commanded-pose deltas are absent (the clutch was out at each segment snapshot), so GAIN cannot be computed either.
* **vr_lag_speed** (`vr_lag_vs_speed.png`): Peak mapper lag vs peak hand speed per protocol segment, from recordings/vr_teleop/protocol_20260826_124739.json (2026-08-26). Under 0.27 m/s every segment stays inside the 30 mm gate; the deliberate fast reach (0.31-0.35 m/s) does not.
* **vr_wrist** (`vr_wrist_20260826.png`): Wrist rotation gain, axis error and lag per segment from recordings/vr_teleop/wrist_20260826_125305.json (2026-08-26, sim-only). Aggregates only -- the file carries no time series, so no orientation-error-over-time curve is possible from it.
* **teleop_motion** (`teleop_motion_generator.png`): Legacy per-joint step clamp vs Ruckig from recordings/baselines/teleop_motion.json (2026-08-23): off-path 51.3/39.2 mm -> 0.0, velocity 12.5x limit -> 1.00x, and the sweep showing the clamp cannot be tuned into compliance.
* **sim_to_real** (`sim_to_real_park_error.png`): Per-joint park-error distribution from recordings/baselines/arm_directional_calibration.json and the joint-terminal compensation from sim_to_real_gap.json (2026-08-23). This is real-hardware data; compensation itself is UNTRIED on hardware.
* **grip_traces** (`grip_traces_t2.png`): Measured knuckle angle over time, T2 S2_full_lift, all five modes (grip_trace.json, clip set 2026-08-23). MEASURED only: the traces carry no commanded channel, so this is not a commanded-vs-measured plot; the plateau is the trace's own.
* **mode_paths** (`mode_path_lengths.png`): End-effector travel per mode and task from mode_difference.json (2026-08-15): T0's left path is 1.407 m under 01 against 2.034 m under 03 for the SAME commanded waypoints. Predates the 2026-08-23 clip re-record and the Ruckig generator.
* **pad_curve** (`pad_offset_curve.png`): Wrist-to-pad offset and finger span against the commanded grip width, both arms, by FK on the mimic chain (2026-08-23). The 2026-08-18 '13.47 mm too long' and T1's '11.43 mm short' compared two different points on this curve, which is why the evidence flipped depending on what opening the simulator was left at.
* **orientation_cost** (`orientation_cost.png`): Mean reach per orientation policy from the home end effector and from the work point (0.4, 0.175, 1.12), with the number of direction walks the WEARER stopped (2026-08-23). From home the pinned wrist costs 89 mm; at the work point it costs 385 mm and 11 of 12 walks are wearer-bound. Freeing the roll alone is worth nothing, and orientation_policy still defaults to 'exact'.
* **wearer_posture** (`wearer_posture.png`): Innermost usable column and home clearance for the five postures in SRL_WEARER_ARMS (2026-08-15). With the wearer's arms deleted entirely the centre is STILL shut, so what closes it is the torso: none binds on torso, down binds on torso, folded binds on L-upperarm. 'behind' and 'out' breach the 150 mm floor at the home pose, which is why a wearer is never asked to adopt them.
* **centre_on_surface** (`centre_on_surface.png`): Every configuration in a 1680-cell search of table height, table distance, object column and overhang, both arms, objects resting on the surface (2026-08-15). Twenty-two cells pass a single IK call; two survive N=10 over the whole path. None is anywhere near the centre, and every survivor needs the object at the very front edge because the pinned wrist arrives from below.
* **budget** (`control_budget.png`): Every term in the positioning budget from control_budget.json (2026-08-23), systematic terms separated from random ones because only the systematic ones can be calibrated away. The pad row is COMPUTED from the shipped constants, so reverting the derivation makes this figure say so. The largest remaining unknown is not on the chart: camera_link vs the physical module
* **reaction** (`reaction_budget.png`): Latency and the distance the end effector covers in it, per motion, from control_budget.json (2026-08-23). The worst case, 'a startle or flinch', eats 1499% of the 150 mm clearance floor before anything can intervene — which is the argument for the floor being 150 mm and not less.
* **language_75** (`language_sweep_75.png`): The full instruction sweep: 75 phrasings, 22 categories (2026-08-18). Zero MISUNDERSTOOD is the result — an utterance that moves the arm to the wrong place is the only unsafe outcome, and there are none, against three for the grammar this replaced. Of the asks, 10 carry a scored reply and all of them resolve correctly; a reply is never counted as CORRECT. The scene is CONSTRUCTED, so this scores grounding and says nothing about detection rate.
* **voice** (`voice_pipeline.png`): Word error rate per utterance and the wake-word threshold sweep (2026-08-17). The parser still returns 14 correct / 5 asked / 21 refused / 0 misunderstood THROUGH this transcription, which is why a voice score in this repository is a PARSER score and never a transcription score. /dev/snd on this host has only 'timer', so no microphone can be opened: one synthetic voice, no accent, no room, no noise, and accuracy against a real speaker is UNMEASURED.
* **predictive** (`predictive_avoidance.png`): Worst clearance at a pose each policy is willing to command, and how many of the 15 steps it will command at all (2026-08-15). This is NOT the file's own min_clearance field, which spans the REFUSED steps and therefore scores a policy at a pose it declined — differencing it reports avoidance making clearance worse, contradicting the file's own control. Scored on commanded poses the result is plain: unassisted, the solver commands poses ~155 mm inside the wearer; with avoidance on, every commanded pose clears the 150 mm floor and the rest are refused by name. The null space itself is worth single-figure millimetres, because it holds the hand fixed by definition and it is the hand that is inside the person. the avoider is exercised against the real solver and the real wearer model, but is NOT wired into ik_follower_node -- that node gates motion near a person and this validation comes first
* **depth** (`depth_pose_accuracy.png`): Object pose error against range for a naive depth centroid and for the fitted estimate, plus the per-pixel depth noise the sensor model implies (2026-08-10). This is the term that enters the control budget as 2.0 mm at grasp range: the 0.082 m figure quoted elsewhere is at 4 m and does not apply at 0.4-0.8 m.
* **capability** (`capability_ladder.png`): What each rung of the degradation ladder costs in end-effector position, replayed through the real recorded master frames, and how many of the channel subsets land on each rung (2026-08-09). The point is that no rung is a cliff and recovery is monotonic — a channel coming back never leaves the system on a lower rung than before it failed.
* **t1_seeds** (`t1_stage2_seeds.png`): Worst pad miss and IK cost for all eight stage-2 seeds (2026-08-18). Splits of 1/3, 2/2 and 3/1 all occur and both arms always work. Seed 0 draws 2/2 and is indistinguishable from stage 1, which is why the recorded clip uses seed 3 — a task that only ever demonstrates its easy case has demonstrated nothing.
* **shared_gain** (`shared_autonomy_gain.png`): Intent inference against a declared object coordinate that is wrong by 0 to 120 mm, 400 trials (2026-08-24). Calibrated perception holds; the declared coordinate collapses past one cube pitch and, worse, becomes CONFIDENTLY wrong rather than ambiguous — the ambiguous fraction falls as the wrong fraction rises. The declared error is swept because in simulation the declared coordinate is the truth, so the sweep is what a real calibration error would do.
* **detector_real** (`detector_real_rgbd.png`): Prompted detection rate and IoU on real RGB-D frames from the LM-O test set, 60 frames and 432 object instances (2026-08-10). Overall 9.0%, on the dataset's own cluttered household and figurine objects — NOT this project's coloured cubes, which the shipped HSV classifier handles at 100% in the renderer. It is the only detection number here measured on real camera data, and it is why that 100% is labelled RENDERED and why an open-vocabulary detector was not adopted: prompted on unseen clutter it fires on a tenth of the objects, and the boxes it does return fit well (IoU mostly above 0.8), so the failure is recall, not localisation.
* **pick_accuracy** (`pick_accuracy.png`): Pad miss against belief error for every pick planned from the measured world map, with the 30 mm capture gate (2026-08-24). The points sit on the diagonal: whatever the pipeline believes, the arm delivers the pads there. That localises the remaining error in perception rather than in the kinematics, which is what makes the unmeasured camera_link extrinsic the most expensive thing still outstanding.
* **mirror** (`home_mirror_residual.png`): Per-link mirror residual at the home pose and the elbow geometry behind it (2026-08-18). The control matters: reflecting the LEFT arm against ITSELF gives 1.2409, so a residual near zero would have been the instrument agreeing with itself rather than a measurement. The asymmetry is structural — the two mounts' base axes mirror exactly but differ by 168° of roll, and two identical arms on mirrored mounts cannot mirror.
* **look_cost** (`look_then_grasp_cost.png`): Where the time goes in a look-then-grasp cycle (2026-08-16). Detection and classification take 0.31 s; the transit to and from the observe pose takes 48.3 s, so the whole optimisation available here is a shorter journey, not a faster detector. Amortised over the 4 picks one look plans, it adds 12.14 s per pick.
* **mount_overlap** (`mount_overlap_sweep.png`): Cells reachable by BOTH arms across 108 swept mount offsets, splays and tilts (2026-08-07). The best configuration anywhere in the sweep reaches 29 shared cells — which is the measurement behind the disjoint-workspace result, and why a two-arm handover task cannot be written against this rig without re-parking an arm in hardware first.

## Refused

* **vr_prefix_misses**: the pre-fix accumulating VR misses (88.1 / 115.4 / 156.2 / 205.9 mm, 2026-08-16) exist only as prose in docs/system/findings.md -- the failed clips were re-recorded in place and no committed scene_events artifact carries the numbers. Refusing to hard-code prose into a figure.
* **vr_session_mcap**: the python 'mcap' library is not importable on this interpreter, so recordings/vr_teleop/session_20260826_123758 (rosbag mcap) stays unread
