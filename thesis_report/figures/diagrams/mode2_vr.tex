% MODE 2 -- DIRECT TELEOPERATION FROM A QUEST HEADSET.
% Same grid as the mannequin diagram. The input stage differs and everything
% from the anchor rightwards is shared, which is the point the figure makes.
\begin{tikzpicture}[x=34mm, y=13mm]

\node[sensor] (ctrl)  at (0, 2.6) {Quest controllers\\6-DOF pose, trigger,\\grip, buttons};
\node[sensor] (hmd)   at (0, 1.2) {headset\\frame period\\\SI{13.9}{\milli\second} @ \SI{72}{\hertz}};

\node[proc]   (unity) at (1, 1.9) {Unity client\\vendor schema\\\texttt{sequence}, \texttt{timestamp}};
\node[proc]   (adb)   at (2, 1.9) {\texttt{adb reverse}\\USB, \texttt{ws://127.0.0.1}\\secure origin};
\node[proc]   (bridge)at (3, 1.9) {vendor bridge\\WebSocket server\\port 8766};

\node[safety] (track) at (3, 3.4) {tracking watchdog\\freeze at \SI{0.20}{\second}};
\node[safety] (link)  at (3, 0.4) {link watchdog\\freeze at \SI{0.30}{\second}};

\node[proc, text width=30mm] (frame) at (4, 1.9)
      {frame conversion\\$p=(x,z,y)$,\\$q=(q_x,q_z,q_y,-q_w)$};
\node[proc]   (clutch)at (4, 0.4) {clutch\\anchors on the arm's\\current EE};

\node[safety] (obs)   at (5, 3.4) {observer e-stop\\absent $\Rightarrow$ refuse\\to drive};
\node[proc]   (anchor)at (5, 1.9) {scale $+$ anchor\\\emph{shared with mode 1}};

\node[safety] (ik)    at (6, 1.9) {collision-aware IK\\clearance floor\\step guard};
\node[actuator](arm)  at (7, 1.9) {Kinova Gen3\\\SI{100}{\hertz} command};

\draw[flow] (ctrl) -- (unity);
\draw[flow] (hmd)  -- (unity);
\draw[flow] (unity)-- (adb);
\draw[flow] (adb)  -- (bridge);
\draw[flow] (bridge) -- (frame);
\draw[flow] (track)  -- (bridge);
\draw[flow] (link)   -- (bridge);
\draw[flow] (clutch) -- (frame);
\draw[flow] (frame)  -- (anchor);
\draw[flow] (obs)    -- (anchor);
\draw[flow] (anchor) -- (ik);
\draw[flow] (ik)     -- (arm);

\draw[fail] (track.east) to[out=0,in=90] node[faillbl, above, pos=0.3] {loss: freeze} (anchor.north);
\draw[fail] (link.east)  to[out=0,in=270] node[faillbl, below, pos=0.3] {loss: freeze} (anchor.south);

\node[font=\scriptsize\itshape, anchor=west] at (0, -0.9)
  {The headset supplies 6~DOF directly, so no spherical decomposition and no
   channel-health layer are needed. Everything from \emph{scale $+$ anchor}
   rightwards is the same code as \cref{fig:mode1}.};
\end{tikzpicture}
